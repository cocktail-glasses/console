apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cocktail-console.fullname" . }}-test-connection"
  labels:
    {{- include "cocktail-console.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cocktail-console.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
